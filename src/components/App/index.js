// == Package imports
import React from 'react';

// == Local imports
// components
import SearchBar from '../SearchBar';

const App = () => {
  return (
    <div className="app">
      <SearchBar />
    </div>
  );
};

export default App;
